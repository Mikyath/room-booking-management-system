# Room Booking System

A simple room booking app built with Node.js and the built-in HTTP module.

## Run locally

```bash
cd backend
node server.js
```

Then open http://localhost:3000 in your browser.
# hotel-management-system
